---
- type: replace
  path: /instance_groups/name=docker/jobs/name=docker/properties?
  value:
    tcp_address: 0.0.0.0
    tls: true
    tls_verify: true
    tls_cacert: "((docker-tls.ca))"
    tls_cert: "((docker-tls.certificate))"
    tls_key: "((docker-tls.private_key))"

- type: replace
  path: /variables?/-
  value:
    name: docker-ca
    type: certificate
    options:
      is_ca: true
      common_name: dockerCA

- type: replace
  path: /variables/-
  value:
    name: docker-tls
    type: certificate
    options:
      ca: docker_ca
      common_name: docker
      extended_key_usage:
      - client_auth
