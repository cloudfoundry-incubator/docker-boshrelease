/var/vcap/sys/log/swarm_manager/*.log {
  missingok
  rotate <%= p('swarm_manager.logrotate.rotate') %>
  compress
  delaycompress
  notifempty
  copytruncate
  size <%= p('swarm_manager.logrotate.size') %>
  su <%= p('swarm_manager.user') %> <%= p('swarm_manager.group') %>
}
